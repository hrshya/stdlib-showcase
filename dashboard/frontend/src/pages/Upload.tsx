import { AppBar } from "../components/AppBar"
import { Form } from "../components/Form"

export const Upload = () => {
    
    return (
        <div>
            <div>
                <AppBar />
            </div>
            <div>
                <Form />
            </div>
        </div>
    );
}

